# AGENTS.md

Agent-facing technical docs for paa-analyzer live in `.agents/context/` — start
at [`.agents/context/index.md`](.agents/context/index.md), then open the
relevant subsystem deep-dive on demand. The binding rules (commands, releasing,
the decision-logging mandate) are in [`CLAUDE.md`](CLAUDE.md).
